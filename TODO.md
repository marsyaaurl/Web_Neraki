# TODO: Add Profile Form Validation

- [x] Add `errors` state to Dialog.jsx
- [x] Create `validateForm` function with validation logic for each field
- [x] Modify `handleSave` to call validateForm and prevent save if errors exist
- [x] Update render to display error messages below inputs when editing
